version: 2

models:
  - name: dm_actor
    columns:
      - name: dm_actor_id
        tests:
          - unique
          - not_null
      - name: first_name
        tests:
          - not_null
      - name: last_name


