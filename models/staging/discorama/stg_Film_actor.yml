version: 2

models:
  - name: stg_Film_actor
    columns:
      - name: actor_id
        tests:
          - not_null
      - name: film_id
        tests:
          - not_null
      - name: last_update
        tests:
          - not_null