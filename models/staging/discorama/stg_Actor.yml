version: 2

models:
  - name: stg_Actor
    columns:
      - name: actor_id
        tests:
          - unique
          - not_null
      - name: first_name
        tests:
          - not_null
      - name: last_name
        tests:
          - not_null
      - name: last_update
        tests:
          - not_null